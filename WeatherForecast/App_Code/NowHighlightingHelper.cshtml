
@helper NowHighlighting(int time) { 
    int now = DateTime.Now.Hour, timenow;
    if (now >= 3 && now < 9)
    {
        timenow = 1;
    }
    else
    if (now >= 9 && now < 15)
    {
        timenow = 2;
    }
    else
    if (now >= 15 && now < 21)
    {
        timenow = 3;
    }
    else
    {
        timenow = 4;
    }
    if (time == timenow)
    {
@:now
    }
}